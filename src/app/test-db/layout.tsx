/**
 * Test Database Page Layout
 * No special layout needed, uses root layout
 */

export default function TestDatabaseLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
